# ============================================================
# GMB SARATHI — SECRET KEYS (.env.local)
# Copy this file into your dev folder, rename it to:  .env.local
# Fill values after each = sign (no quotes, no spaces around =)
# NEVER commit this file to GitHub (.gitignore must contain .env*)
# ============================================================

# ---------- DataForSEO (the only paid service) ----------
DATAFORSEO_LOGIN=your-email-here
DATAFORSEO_PASSWORD=api-password-from-dashboard

# ---------- Supabase (new key system: publishable/secret) ----------
SUPABASE_URL=https://tiracrhjhbzvbqmaunrh.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxx
SUPABASE_SECRET_KEY=sb_secret_xxxxx
# (server-only; bypasses RLS — never expose in frontend)

# ---------- AI (Groq first, OpenRouter free models fallback) ----------
GROQ_API_KEY=gsk_xxxxx
OPENROUTER_API_KEY=

# ---------- Google (no billing anywhere) ----------
PSI_API_KEY=
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# ---------- Cloudflare Turnstile (public checker) ----------
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# ---------- WhatsApp (fill when Veblika account arrives) ----------
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_WABA_ID=
WHATSAPP_ACCESS_TOKEN=

# ---------- App guards ----------
DAILY_SPEND_CAP_USD=1.00
PUBLIC_CHECKER_DAILY_LIMIT=50
PUBLIC_CHECKER_PER_IP_LIMIT=3
