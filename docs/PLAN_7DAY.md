# 7-Day Sprint Plan (Day 1 = 12 Jul 2026 · Day 7 = 18 Jul; 19 Jul is buffer)

Owners: **B** = backend agent · **F** = frontend agent · **M** = main agent · **Y** = Yogesh (client).
Milestone definitions + exit criteria: [MILESTONES.md](MILESTONES.md). Merge policy at the bottom.

| Day | Deliverables |
|---|---|
| **1 (Sat 12) — CLOSED** | **M ✅ SHIPPED**: M0 complete + adversarially verified — scaffold, schema+seed (applied to live DB), spend guard (atomic reserve, 24 tests green incl. live RLS), contract EP-001..022, CI, briefs, worktrees; `m0:verify` **12/12** incl. live DataForSEO ping → spend_ledger (M0 exit criterion met); PM verification fixes committed (grants migration 0004, script path fixes, RLS test flavours, CLAUDE.md v1.8). **F ⏩ CARRY-OVER → Day 2**: app shell + P1 Dashboard + P2 New Audit (no Day-1 commits on `agents/frontend`). **B ⏩ CARRY-OVER → Day 2**: dataforseo.service client + score.service skeleton + fixture parser (no Day-1 commits on `agents/backend`). |
| **2 (Sun 13) — CLOSED** | **B ✅ M1 DONE + AHEAD (4 PRs)**: fixture parser · score.service (§2.5) · dataforseo.service (all 6 §2.6 endpoints guarded + preview overloads) · **full M1 audit pipeline: resolver, reviews/posts normalize, competitor discovery, category intel, link pack (27), bilingual tokenizer, sanity checks, persistence, report, top-fixes + EP-001/002/013/015 + `/api/businesses` + `/api/reviews`**. **🎯 M1 EXIT VERIFIED by MAIN** — `tests/manovedh-fixture.test.ts` (13 assertions) reproduces the fixture end-to-end: 41 amber, exact rubric, phone/category/services/hours/reply/posts/NAP/subdomain flags, review stats, all six sanity flags, EP-002 payload. **F ✅ SHIPPED + AHEAD (2 PRs)**: app shell + P1 Dashboard + P2 New Audit + **P3 Audit Report** (gauge, 10-row rubric/mobile accordions, fixes मराठी–EN toggle+edit, 27-link pack, WhatsApp modal, Mark-as-Client, mobile sticky bar) + 14-screen scaffold + shared UI kit + design-system page; typed mocks incl. full EP-002 `AuditReport`. **M ✅**: merged **5 PRs** (3 B + 2 F) → `main` `7d77bad`, each re-gated (**107 tests / typecheck / lint / build** all green), arbitrated 2 contract gaps (`resolve` + `dashboard/stats` + `BusinessCandidate`/`DashboardStats` types), local CI substitute (Actions still disabled). **M1 complete a day early.** |
| **3 (Mon 14) — CLOSED · full scope done** | **B ✅ M1.5 + M2 DONE (3 PRs)**: M1.5 website audit (crawler + PSI + renorm, EP-014) **with SEC-001 SSRF (P0) verified — 55 attack-case tests green**; M2 grid/teleport (EP-003/004 + history + compare) with **all 3 gates passed** (cost preview, no unguarded calls, task_post idempotency) — reworked mid-day to the locked `top_ranks` contract after an initial deviation; + the 2 early-integration ₹0 endpoints (`resolve`, `dashboard/stats`). **F ✅ P4+P3b+P6+P5 DONE (3 PRs)**: Competitor Compare, Website Audit detail, Review Inbox, **P5 Grid on real Leaflet+OSM**, + typed API layer (`useApiGet` + `LIVE_ENDPOINTS` registry for the Day-5 flip). **M ✅**: **7 PRs merged** (#7–#13) → `main` `072141b`, each re-gated (**202 tests** green), locked the grid/website contract pre-build, arbitrated 4 contract-proposals (`resolve`, `dashboard/stats`, `psi_desktop`, `RankEntry.area`), fixed 2 divergence seams (backend + frontend grid), demo-flush plumbing shipped. **⛔ STILL BLOCKED: DataForSEO UNVERIFIED** — everything remains fixture-verified only until Yogesh verifies at app.dataforseo.com; Actions still off (local CI substitute). |
| **4 (Tue 15) — CLOSED · MVP GATE (automated) MET** | **B ✅ M3 + M4 DONE (3 PRs)**: M3 AI layer (Groq→OpenRouter chain, 7 tools + fixes redraft, EP-005/015) **with SEC-002 prompt-injection — verified via hostile-review corpus + validator + approved=false persist**; M4 pdf.service (Playwright behind FEATURE_PDF) + wa.service stub (FEATURE_DISABLED) **with SEC-003 XSS→PDF — verified (esc + CSP + bundled Devanagari font, no external fetch)**. **F ✅ P7 + P8 DONE (2 PRs)**: Post Audit + AI Tools (7 tabs + Category Finder, gated publishing). **M ✅**: **5 PRs merged** (#13-#17) → `main` `b00cb32`, **229 tests**; **ran the MVP gate end-to-end** — fixture audit → real chromium PDF (240KB, %PDF-1.4, extractable मनोवेध + 41 + Marathi) → saved `tmp-mvp/manovedh_audit_mr.pdf`. arbitrated `"fixes"` AiToolType. **🎯 MVP GATE MET** (Yogesh approved the Marathi PDF 16 Jul — both halves pass). **⛔ DataForSEO STILL 403 (40104)** — email verification wasn't enough; needs in-panel/support unlock (nothing live-verified). |
| **5 (Wed 16) — CLOSED** | **Integration day.** DB CURRENT (migrations applied + verified live); MVP GATE MET. **📌 DECISION: DataForSEO DEFERRED to post-funding** (needs $50 deposit — see `docs/DATAFORSEO_DEFERRAL.md`). **Integration = ₹0/DB flips only** (stats/businesses/spend/reviews → live seed data). **PAID screens (resolve/audit/posts/grid/website) stay MOCK behind the CR-1 kill-switch (default OFF) — no rework, code stays as-is, dormant.** CR-1 shipped + merged (migration + type + server enforcement, fail-safe OFF). **CR-2/CR-3** still pending PM specs. **M**: merge/gate ₹0 flips, own seams; **F**: flip ₹0 keys one commit each; **B**: stand down on DFS (no more live smoke until funded). |
| **6 (Thu 17) — CLOSED** | **Contract-first + merge-gate day.** **M ✅**: locked the **EP-021/022 P12 contract** (research→design→3-lens adversarial critique — caught done-while-unapproved, prose-only baseline immutability, complete→paid-audit, bypassable one-active) → hardened `@/types/sprint.ts` + migration `20260717000001` (**client-applied + MAIN-verified**: 4 fix_tasks columns + baseline NOT NULL + one-active unique index + write-once immutability trigger — tamper test rejects). Merged the clean Day-6 work via cherry-pick (**P9 + P11 + B2/B3 + backend fix-list**, `2b0966d..6d62d6b`, 262 tests / CI green). **BOUNCED P12** (both agents built pre-lock → 16 typecheck-drift errors; functionally solid — SHAPE mismatch) with an exact rebase+adapt list. Arbitrated 5 contract asks (+`GET /api/sprint/:id`, +2 P9 rows, +`GET /api/spend/ledger`; reconciled `manual_links`/`details.reasons` onto the locked shapes). Flipped **`/api/settings` LIVE** (CR-1 toggle persists: migration + B2 field-name fix). **B ✅**: M6 sprint engine (server-side US-024 gate w/ reasons, immutable baseline, **zero-vendor calls proven with poisoned fetch**) + P9 reads + fix-list (liveGate required, TTL flag-read); engine shape-adapting to the lock. **F ✅**: P9+P11 full + B2/B3 fixes **merged**; P12 UI built (bounced with backend, adapting). **Y ✅**: applied migration `20260717000001` + set `FEATURE_PDF=on`; UAT tonight. **Evening turnaround:** both agents rebased+adapted P12 to the locked shapes → MAIN gated (typecheck 0 / 292 tests / zero GBP-writes) + **merged (`1b182da`) + flipped `/api/sprint` + `/api/report` LIVE** (EP-006 verified e2e, 3 langs; manovedh audit backfilled). P12 loads live (seeded sprint verified). **Carry → Day 7:** `GET /api/spend/ledger` build + seed-wide snapshot backfill (7 audits). |
| **7 (Fri 18)** | **B**: M7 public checker (EP-008/009, Turnstile, 3/IP/day + 50/day + 1 report/phone/day) + security pass — **live info-pull dormant behind the CR-1 flag; demos on sample data until funded**. **F**: P10 public page (Marathi-first) + P0 polish + mobile pass (<920px). **M**: hardening checklist §1.8 (minus VPS + the live-audit items — deferred), deploy to **Vercel**, smoke-test prod. **Go-live data cutover: ① `npm run flush:demo -- --yes` → ② delete `app/public/dev`.** ③ *The "one real audit vs live Google" step moves to the future-activation checklist (`docs/DATAFORSEO_DEFERRAL.md`) — runs when the $50 deposit lands.* |
| **Week 2** | GBP OAuth publishing + WhatsApp live when keys/approval arrive (flags flip on); M9 ops layer after first paying clients (per blueprint calendar rule). |

### Pre-launch (post-funding) — DataForSEO activation
DataForSEO live calls are **deferred** (needs a $50 deposit — see [`DATAFORSEO_DEFERRAL.md`](DATAFORSEO_DEFERRAL.md)).
When the founder funds it, run the 5-step **future-activation checklist** in that doc
(fund → `RUN_LIVE_SMOKE` returns 20000 → apply `20260716000001` → `PATCH /api/settings
{dataforseo_live_enabled:true}` → flip the paid `LIVE_ENDPOINTS` keys), then the launch
checklist §1.8 "3 real audits vs live Google".

## Merge policy (MAIN agent enforces)

1. Only the MAIN agent merges to `main`; both agent branches merge at least once daily
   (more on Day 5).
2. CI (typecheck + lint + test) must be green on the branch before merge.
3. A milestone's exit criteria are verified by the MAIN agent BEFORE the next milestone
   starts (fixture test for M1 is non-negotiable).
4. Agents commit small and push `agents/backend` / `agents/frontend` at least twice daily;
   never touch `main` or the other agent's folders (.claude/rules/ownership.md).
5. Contract changes: `contract-proposal:` note → MAIN edits `src/types` + API_CONTRACT.md
   → both agents pull.
